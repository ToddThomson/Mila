# Development override for the VS Code dev container (see .devcontainer/devcontainer.json).
# Layered on top of docker-compose.yml, which provides the build, GPU reservation, and the
# ..:/mila bind mount. Keep the container running so the editor can attach to it.
services:
  mila-dev:
    command: sleep infinity
